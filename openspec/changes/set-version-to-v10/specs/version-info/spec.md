## ADDED Requirements

### Requirement: Display app version in settings
The system SHALL display the current app version in the settings screen so users can identify which version they are running.

#### Scenario: Version visible in settings
- **WHEN** user navigates to the Settings screen
- **THEN** the app version is displayed (e.g., "Version 1.0.0")